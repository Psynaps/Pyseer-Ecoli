While in the directory where this was was downloaded, which contains all the .fa files and unitig_file_list.txt run the following command:
unitig-counter -unitig_file_list.txt -output output -nb-cores 4
